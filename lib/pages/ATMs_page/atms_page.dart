import 'package:flutter/material.dart';

class AtmsPage extends StatelessWidget {
  const AtmsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("atms page"),
      ),
    );
  }
}
