import 'package:flutter/material.dart';

class MoneyboxPage extends StatelessWidget {
  const MoneyboxPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("MoneyboxPage page"),
      ),
    );
  }
}
